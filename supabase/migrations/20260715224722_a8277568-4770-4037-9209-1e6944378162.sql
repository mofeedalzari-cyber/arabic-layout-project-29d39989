-- Redacted: this migration previously contained a real admin account seed
-- with a plaintext password. The credential has been rotated and the seed
-- was removed from source control. Admin accounts must be provisioned
-- out-of-band; do not seed real credentials in committed migrations.
SELECT 1;
